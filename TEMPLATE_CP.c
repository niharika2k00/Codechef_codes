
//LINKS ::

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define ll long long
#define loop1(n) for (ll i = 0; i < (n); i++)
#define loop2(n) for (ll i = 1; i <= (n); i++)
// #define test ll t; cin>>t; while(t--)
#define test           \
    ll t;              \
    scanf("%lld", &t); \
    while (t--)

int main()
{
    // serve your dish
   

    return 0;
}
